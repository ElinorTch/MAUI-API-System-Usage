<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.vBatterie"
             Title="vBatterie">
    <VerticalStackLayout>
        <Label 
            FontSize="22"
            TextColor="{StaticResource Primary}"
            Text="Info alimentation"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Button Text="" Clicked="Alimentation_click"/>
        <Label 
            FontSize="22" TextColor="{StaticResource Secondary}"
            x:Name="lb_Alim"
    Text=""
    VerticalOptions="Center" 
    HorizontalOptions="Center" />
        
    </VerticalStackLayout>
</ContentPage>